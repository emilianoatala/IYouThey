type Post {
    id:ID
    description: String
    createdAt: String
}

type Subscription {
    newPost:Post
}

type Query {
 getAllPosts : [Post]
}

input PostInput {
    id:ID
    description: String
    createdAt: String
}

type Mutation {
    setPost(input:PostInput): Post
}